# Force Nixpacks to use Node 22 (openai 7 / vite 7 / cross-env 10 require it).
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[variables]
NIXPACKS_NODE_VERSION = "22"
NPM_CONFIG_UPDATE_NOTIFIER = "false"
